/*! \file main.cpp
	*
	* \brief Kod programu testującego klasę Punkt2
	*
	* Plik zawiera funkcję main(),
	* w której wykonano kilka podstawowych testów
	* dotyczących klasy Punkt2
	*
	* \author Jan Nowak
	* \date 2000.0.01
	* \version 1.00.00
	*/

#include "Punkt2.h"
#include "Polygon.h"
#include <iostream>

using std::cout;

int main()
{
	/*
	Punkt2 p1 = Punkt2(5.0, 0.6);

	p1.print();

	p1.setX(1.0);
	p1.setY(1.0);

	p1.print();

	

	cout << "Punkt p1 = " << p1 << '\n';

	Punkt2 p2(-2, 5);

	cout << "Punkt p2 = " << p2 << "\n";

	cout << "d(p1, p2) = " << p1.getDistance(p2) << '\n';

	// auto p3 = p1.addVectors(p2);
	auto p3 = p1 + p2;

	cout << "p3 := p1 + p2 = " << p3 << '\n';

	cout << "3 * p3 = " << p3.getScaledVector(3) << '\n';
	// << "3 * p3 = " << p3 * 3 << '\n';

	cout << "długość wektora 3 * p3 + [8, -6] = "
		 << (p3.getScaledVector(3).addVectors(Punkt2(8, -6))).getLength() << '\n';
	// << (p3 * 3 + Punkt2(8, -6)).getLength() << '\n';

	cout << "<p1;p2> = p1_x * p2_x + p1_y * p2_y = "
		 << p1.getDotProduct(p2) << '\n';

	cout << "max(" << p2 << ") = " << p2.maximumNorm() << '\n';

	cout << "kąt między " << p1 << " i [1; 0] (45°) = " << p1.angleBetween(Punkt2(1, 0)) << " rad\n\n";

	return 0;
*/

Polygon p;
}